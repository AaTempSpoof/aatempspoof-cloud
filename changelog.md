# AaTempSpoof v15.5 正式版更新公告

## 开机与运行稳定性

- 新增「系统早期启动伪装」开关，默认关闭。开启后仅在系统服务完成前接管 CPU、GPU 和内存温度，减少开机阶段短暂读到真实高温的情况。
- 早期伪装受独立完整性和授权门禁保护；授权暂时不可用时最多维持 3 分钟，正式进程启动后会无缝交接，超时或异常时自动清理并恢复。
- 首次热控接管会等待 `thermal zone` 注册稳定后再继续，并增加节点读回、温控模式和 trip 点写入校验；写入失败会记录诊断并回滚，降低开机卡顿、温度节点失控和错误改写的风险。
- 修复部分 TSENS 温控节点的 IRQ / trip 处理问题，关闭伪装、主程序退出、停用或卸载时恢复原始温控状态。

## 内核温度墙与安全桥接

- 将电池上层、电池底层、USB 温度保护、MOS 和充电 PMIC 结温统一到「内核伪装温度墙」总开关，并保留统一风险确认；开启后会自动避让电池动态伪装和充电拟真，避免多条链路互相覆盖。
- 动态 `opbatt` 桥接增加模块 ABI、KMI、布局、KCFI 和目标函数证据校验，加载后执行真实读回自检并进入安全观察期；验证不通过时自动拒绝或隔离，不冒险改写未知内核结构。
- 新增桥接状态、事件记录、加载器结果和端到端温度读回诊断；完全启动后可在 WebUI 中发起一次手动加载，失败仍保持安全隔离。
- 内核桥接仅在对应版本和本机结构全部匹配时启用；未适配设备只停用内核温度墙，普通温度伪装、统计和其他功能不受影响。

## 充电统计与快捷控制

- 充电统计页继续记录电池功率、充电器功率、电流、电压、电量、容量、循环次数、真实温度和伪装温度，优化实时曲线、历史记录、单双电换算和长图导出。
- 充电岛改用模块统计服务的最新有效样本，充电状态和充满判断优先读取物理电源节点；拔出充电器、确认充满或服务停止后及时撤销通知，避免残留功率或重复通知。
- WebUI 统计页与充电岛共享单双电和真实温度显示设置，设备无法可靠提供真实温度时自动隐藏该项并提示，不把伪装温度冒充真实温度。

## 配置迁移与默认恢复

- 覆盖升级时扩展配置迁移范围，支持迁移模块配置、应用 `shared_prefs`、`files`、`databases` 和设备身份；升级失败会尝试恢复旧应用与原配置。
- 检测到旧配置时提供迁移或使用 v15.5 默认配置的选择，运行日志、临时状态和桥接隔离标记不会被误当作用户配置迁移。
- 「恢复默认配置」现在同时处理模块可调配置、应用开关和界面设置，并保留 Android Keystore 设备身份、云端授权和充电统计记录。

## WebUI 外观与交互

- 优化状态、配置、统计、附加和关于页面的响应式布局，修复横向二级菜单、统计曲线操作区、卡片显示和页面切换时的错位问题。
- 页面切换支持「弹出」和「平移」两种方式；弹窗、轻提示和充电记录统一入场效果、滚动边界和触感反馈。
- 液态玻璃默认开启，模糊范围与滑块状态统一；纯黑、纯白主题下隐藏液态玻璃设置，所有卡片和弹窗改用对应主题的平面背景与描边。
- 默认外观改为纯白且不再内置默认壁纸；自定义图片、触感强度、底栏液态玻璃和其他界面设置可在关于页调整。

## 卸载体验

- 保留 WebUI 二次确认、一次性卸载授权、进程停止、节点恢复、配套应用卸载和模块目录清理。
- 卸载载荷确认 `AAaTempSpoof` 的活动目录和更新目录均已删除后，若 KernelSU 管理器正处于前台，会自动重启管理器主界面刷新模块列表；管理器在后台时不会被强行拉起。
