# AaTempSpoof v15.6 正式版更新公告

## 快捷开关应用

- 应用彻底去界面化：移除主界面与桌面入口，去掉网络权限与 AndroidX 依赖，主题改用系统无标题栏主题。现在它的全部功能就是控制中心磁贴、充电岛通知与 LSPosed 真实温度 Hook，APK 体积由约 745 KB 降到约 42 KB。
- 充电岛改为与 WebUI 统计页同源读取电池功率与电压：直接读取电池电源节点的状态、电流与电压并按单双电系数换算，节点不可用时才回退到充电统计样本，减少充电中显示「系统未提供」的情况。

## Scene 真实温度 Hook

- 适配最新版scene

## 内核温度墙与桥接

- 兼容 opbatt 8.2.18 及以上：状态节点由 `/proc/opbatt/chg_state` 迁移到 `/dev/opbatt_state`，改由 `opbatt_httpd` 以 `POST /v1/chg-state`（text/csv）提供。模块与 WebUI 现在优先读旧节点，旧节点缺失时自动回退到新接口，端口自动从 fopbatt 的 `service.sh` 解析（缺省 28765）。
- 桥接的端到端温度读回与诊断导出新增「读取源（proc / httpd）」标注，新旧两代 opbatt 都能验证伪装是否真正生效。
- 桥接安全策略不变：仍按模块 ABI、KMI、布局、KCFI 与目标函数证据校验，验证不通过时自动拒绝或隔离，只停用内核温度墙，不影响普通温度伪装与统计。