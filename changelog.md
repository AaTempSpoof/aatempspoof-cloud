# AaTempSpoof v15.0 正式版更新公告

## 欧加真内核级无上限温度墙

- 将 `Battery Gauge`、`Virtual Gauge`、`USB NTC`、`Charge Pump MOS` 与 `Charging PMIC` 的温度数据统一纳入内核伪装链路。
- 对 `power_supply`、`oplus_chg` 与 `virtual_ic` 的充电温控读数路径执行内核态重写，使充电策略直接读取 AaTempSpoof 输出的温度数据。
- 通过内核温度数据路径接管解除欧加真固定充电温度墙，上限可设置为无限制；生效位置是内核充电决策链，不是 WebUI 显示层。

## 真实温度与充电统计

- 优化真实电池温度识别，恢复双温度传感器设备读取，并修复真实温度超过 80℃ 后停止更新的问题。
- 无法可靠获取真实温度的设备会自动回退或隐藏，不再把伪装温度显示为真实温度。
- 充电统计的单双电芯设置与控制应用保持同步，v15.0 默认使用双电芯模式，仍可在 WebUI 中自行切换。
- 充满判断改为读取系统实际充满状态，不再单纯以电量达到 100% 判断；真正充满后停止继续记录功率曲线。
- 温度图表新增用户自定义温度墙和撞墙断充状态。
- 优化历史记录、统计长图导出、图表刻度显示、页面滚动和数据刷新流畅度。

## 充电岛与快捷控制应用

- `com.aatempspoof.tile` 从单一快捷磁贴扩展为快捷控制、充电岛与真实温度显示应用。
- 新增 Android 原生充电实时通知，可在支持的系统上显示流体云／灵动岛，实时展示充电功率、电量和真实温度。
- 充电岛左侧可显示真实温度；关闭真实温度或设备不支持时自动恢复原充电电池图标。
- 充电岛会跟随模块的单双电芯设置，系统确认充满后自动消失。
- 加强开机、接入电源和后台被清理后的自动拉起，缩短插入充电器后出现充电岛的等待时间。
- Root 权限改为持续检测，授权成功后应用与快捷磁贴无需清理后台即可立即更新。
- 应用内新增桌面图标显示开关和充电岛开关；WebUI 关于页也可直接开启或关闭充电岛。
- 刷入模块时会覆盖安装最新版应用并尽量保留用户设置，请勿手动删除 `com.aatempspoof.tile`。

## LSPosed 真实温度显示

- 检测到 LSPosed 和受支持应用时，会自动启用 AaTempSpoof LSP 模块并配置对应作用域。
- 支持让 Scene、`com.example.chargingisland` 和 OShin 状态栏左侧显示 AaTempSpoof 获取的真实温度。
- OShin 状态栏兼容单排与双排显示，只替换温度数字，不改变其他状态栏内容。
- Scene 当前以 **9.3.8** 为专用适配版本，其他版本可能无法正常替换温度显示。
- WebUI 关于页新增「Hook 真实温度」开关；关闭后上述应用恢复原来的温度显示。

## WebUI 更新

- 内核温度相关开关和真实断充阈值整合到同一张「内核伪装温度墙」卡片。
- 页面切换新增「弹出／平移」两种动画，可在关于页自行选择。
- 优化连续快速切换页面时的响应，修复页面偏移、边缘裁切、滚动位置继承和标题闪现。
- 优化液态玻璃显示性能，降低页面切换和滚动时的卡顿。
- 修复自定义壁纸无法保存、默认壁纸进入时白屏和部分 WebUI 管理器无法持久保存壁纸的问题。
- 关于页可直接管理充电岛、真实温度 Hook、更新检查、诊断导出和模块卸载。
- 修复 WebUI `Load failed`、底栏短暂无响应、充电岛开关状态读取失败及部分文字或开关被裁切的问题。

## 安装与卸载

- 刷入前会检测是否仍连接充电器；检测到充电时停止安装并提示拔掉充电线。
- 已安装旧模块时自动沿用原模块名称并直接安装；只有首次安装且无法识别旧名称时才需要音量键选择名称。
- 刷入时自动覆盖安装控制应用，并尝试配置通知、后台运行和 LSPosed 作用域。
- 关于页新增无残留卸载按钮；充电状态下会阻止卸载，断开充电器后可执行，完成后提示重启。
- 优化 Horae 共存与灭屏充电表现，减少亮屏满功率、灭屏后充电功率下降的情况。

## 重要风险提示

- 温度伪装只改变系统读取到的数值，**不会降低设备真实温度**。
- 同时伪装电池、USB、MOS 和充电 PMIC 温度后，原有限流、降功率或断充保护可能延后或失效。
- 高温充电可能造成电池加速老化、鼓包、漏液、主板或接口损坏、异常重启、冒烟、燃烧、起火甚至人身伤害。请仅在充分理解风险并能够持续监控真实温度时使用。
